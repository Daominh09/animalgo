# Copy to mobile/.env.
#
# One variable, because the app talks only to our backend — including for sign-in. The
# Supabase URL and anon key live in backend/.env now and never reach the client.

EXPO_PUBLIC_API_URL=http://localhost:8000
