import { View, Text } from "react-native";

// Owner: Person A — Capture & Species ID
// Week 1: camera permission flow + basic capture screen (expo-camera)
// Week 2: real upload to R2 + DeepSeek vision call, confidence branching, manual-confirm UI

export default function CameraScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text>Camera screen — Person A starts here (Week 1)</Text>
    </View>
  );
}
