import { View, Text } from "react-native";

// Owner: Person C — Battle System
// Week 1: roster-select + challenge screen with mock captures
// Week 2: wire to real captures + real challenge/resolution endpoints

export default function BattleScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text>Battle screen — Person C starts here (Week 1)</Text>
    </View>
  );
}
