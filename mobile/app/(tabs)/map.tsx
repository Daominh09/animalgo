import { View, Text } from "react-native";

// Owner: Person B — Rarity Engine & Collection
// Week 1: MapLibre shell, no real pins yet
// Week 2: wire to real captures, verify fuzzed coords render correctly for sensitive species

export default function MapScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text>Map screen — Person B starts here (Week 1)</Text>
    </View>
  );
}
