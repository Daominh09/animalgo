# Used only by docker-compose.yml (not by the app itself) to tell the
# mobile container's Expo dev server your machine's real LAN IP, so the
# QR code it prints is something your phone can actually reach.
# Find yours with `ipconfig getifaddr en0` (macOS) and copy this to `.env`.
HOST_LAN_IP=
